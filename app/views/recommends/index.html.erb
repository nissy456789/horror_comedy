<div class="flex flex-col items-center min-h-screen bg-gray-700 text-white">
<h2 class = "text-white" >あなたへのおすすめ映画</h2>

<% if @recommend_movies.present? %>
  <ul>
    <% @recommend_movies.each do |movie| %>
      <li>
        <%= link_to movie.title, movie_path(question) %>
        <p><%= truncate(movie.content, length: 100) %></p>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>おすすめの映画が見つかりませんでした。</p>
<% end %>
</div>