<%= form_with url: watched_path(movie_id: @movie.id), method: :delete, local: true, id: "unwatch-form-for-movie-#{@movie.id}" do |form| %>
  <%= button_tag type: 'submit', class: 'hover:bg-orange-500 rounded-lg block text-black flex items-center bg-orange-400 px-2 py-1 text-xl' do %>
    <%= image_tag("iine.jpg", class: "w-6 h-6 mr-2") %> 視聴済み
  <% end %>
<% end %>
