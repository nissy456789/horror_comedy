<%= form_with url: watched_path(movie_id: @movie.id), method: :delete, local: true, id: "unwatch-form-for-movie-#{@movie.id}" do |form| %>
  <%= form.submit '観てない', class: 'rounded-lg block text-black flex items-center inline-block bg-orange-400 px-2 py-1 text-xl' %>
<% end %>