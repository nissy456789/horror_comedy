<%= form_with url: unbookmark_movie_path(movie_id: @movie.id), method: :delete, local: true, id: "unbookmark-form-for-movie-#{@movie.id}" do |form| %>
  <%= form.submit '観たくない！！', class: 'hover:bg-orange-500 rounded-lg block text-black flex items-center inline-block bg-orange-400 px-2 py-1 text-xl' %>
<% end %>